# 企业微信教务系统 - 完整实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在12周内完成一个完整的教务系统，包含企业微信集成、报名缴费、排课考勤、微信/支付宝小程序支付功能。

**Architecture:** 采用前后端分离架构，后端FastAPI + SQLModel + PostgreSQL，前端Vue 3 + Element Plus，移动端uni-app小程序。分层设计：API → Service → Repository。

**Tech Stack:** Python 3.11+, FastAPI 0.110+, SQLModel, PostgreSQL 15+, Redis 7+, Vue 3.4+, uni-app, 微信支付, 支付宝

---

## 📅 里程碑总览

| 周次 | 里程碑 | 核心交付物 |
|------|--------|------------|
| 第1周 | 环境搭建完成 | Docker环境、企业微信对接、基础认证 |
| 第2周 | 数据层完成 | 所有ORM模型、Alembic迁移、基础CRUD |
| 第3周 | 课程管理完成 | 课程CRUD、教室管理、校区管理 |
| 第4周 | 学员管理完成 | 学员CRUD、标签管理、来源跟踪 |
| 第5周 | 合同缴费完成 | 合同管理、课时包购买、缴费记录 |
| 第6周 | 支付集成完成 | 微信支付、支付宝集成、回调处理 |
| 第7周 | 排课考勤完成 | 排课、冲突检测、考勤打卡 |
| 第8周 | 教师端完成 | H5页面、课表查看、考勤操作 |
| 第9周 | 消息系统完成 | 定时任务、消息推送、到期提醒 |
| 第10周 | 小程序A完成 | 课表、考勤、作业查看 |
| 第11周 | 小程序BC完成 | 缴费、请假、调课功能 |
| 第12周 | 测试部署完成 | 完整测试、生产环境部署 |

---

## Week 1: 环境搭建

### 任务 1.1: 创建项目目录结构

**Files:**
- Create: `backend/app/__init__.py`
- Create: `backend/app/main.py`
- Create: `backend/app/config.py`
- Create: `backend/requirements.txt`
- Create: `frontend/admin/package.json`
- Create: `deployment/docker-compose.yml`

**Step 1: 创建基础目录结构**

```bash
mkdir -p backend/app/{api/v1,core,models,schemas,services,crud,tasks}
mkdir -p backend/tests
mkdir -p frontend/admin/src/{api,components,views,stores,router}
mkdir -p deployment
mkdir -p docs/plans
```

**Step 2: 创建requirements.txt**

```txt
# backend/requirements.txt
fastapi==0.110.0
uvicorn[standard]==0.27.0
sqlmodel==0.0.20
alembic==1.13.1
psycopg2-binary==2.9.9
redis==5.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
httpx==0.26.0
apscheduler==3.10.4
celery[redis]==5.3.6
python-dotenv==1.0.1
pydantic==2.6.0
pydantic-settings==2.1.0
pytest==7.4.4
pytest-asyncio==0.23.3
```

**Step 3: 创建FastAPI应用入口**

```python
# backend/app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.core.config import settings
from app.api.v1 import auth, courses, students, schedules

app = FastAPI(
    title="企业微信教务系统API",
    version="1.0.0",
    docs_url="/docs",
    redoc_url="/redoc"
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(auth.router, prefix="/api/v1/auth", tags=["认证"])
app.include_router(courses.router, prefix="/api/v1/courses", tags=["课程"])

@app.get("/health")
async def health_check():
    return {"status": "ok"}
```

**Step 4: 创建配置文件**

```python
# backend/app/core/config.py
from pydantic_settings import BaseSettings
from typing import List

class Settings(BaseSettings):
    # 应用配置
    APP_NAME: str = "企业微信教务系统"
    DEBUG: bool = True

    # 数据库配置
    DATABASE_URL: str = "postgresql://edu:edu123456@localhost:5432/edu_wework"
    DATABASE_URL_DEV: str = "sqlite:///./edu_dev.db"

    # Redis配置
    REDIS_URL: str = "redis://localhost:6379/0"

    # JWT配置
    SECRET_KEY: str = "your-secret-key-change-in-production"
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 60 * 24  # 24小时

    # 企业微信配置
    WEWORK_CORP_ID: str = ""
    WEWORK_CORP_SECRET: str = ""
    WEWORK_AGENT_ID: str = ""

    # CORS
    CORS_ORIGINS: List[str] = ["http://localhost:5173", "http://localhost:3000"]

    class Config:
        env_file = ".env"

settings = Settings()
```

**Step 5: Commit**

```bash
git add backend/ deployment/ frontend/
git commit -m "feat: 创建项目基础结构和配置文件"
```

---

### 任务 1.2: 企业微信OAuth2认证集成

**Files:**
- Create: `backend/app/core/wework.py`
- Create: `backend/app/services/auth_service.py`
- Create: `backend/app/api/v1/auth.py`
- Test: `backend/tests/test_auth.py`

**Step 1: 创建企业微信服务类**

```python
# backend/app/core/wework.py
from typing import Optional
from datetime import datetime, timedelta
import httpx
from app.core.config import settings
import logging

logger = logging.getLogger(__name__)

class WeWorkService:
    """企业微信API服务"""

    def __init__(self):
        self.corp_id = settings.WEWORK_CORP_ID
        self.corp_secret = settings.WEWORK_CORP_SECRET
        self.agent_id = settings.WEWORK_AGENT_ID
        self._access_token: Optional[str] = None
        self._token_expire: Optional[datetime] = None

    async def get_access_token(self) -> str:
        """获取access_token（带内存缓存）"""
        if self._access_token and self._token_expire:
            if datetime.now() < self._token_expire:
                return self._access_token

        url = "https://qyapi.weixin.qq.com/cgi-bin/gettoken"
        params = {
            "corpid": self.corp_id,
            "corpsecret": self.corp_secret
        }

        async with httpx.AsyncClient() as client:
            response = await client.get(url, params=params, timeout=10.0)
            data = response.json()

            if data.get("errcode") == 0:
                self._access_token = data["access_token"]
                # 提前5分钟过期
                self._token_expire = datetime.now() + timedelta(
                    seconds=data["expires_in"] - 300
                )
                logger.info("成功获取企业微信access_token")
                return self._access_token

            logger.error(f"获取access_token失败: {data}")
            raise Exception(f"获取access_token失败: {data}")

    async def get_user_info(self, code: str) -> dict:
        """通过授权码获取用户信息"""
        token = await self.get_access_token()

        # 1. 获取user_id
        async with httpx.AsyncClient() as client:
            response = await client.get(
                "https://qyapi.weixin.qq.com/cgi-bin/auth/getuserinfo",
                params={"access_token": token, "code": code},
                timeout=10.0
            )
            data = response.json()
            if data.get("errcode") != 0:
                raise Exception(f"获取用户ID失败: {data}")
            user_id = data["UserId"]

        # 2. 获取用户详情
        async with httpx.AsyncClient() as client:
            response = await client.get(
                "https://qyapi.weixin.qq.com/cgi-bin/user/get",
                params={"access_token": token, "userid": user_id},
                timeout=10.0
            )
            return response.json()

    async def send_message(self, user_id: str, content: str) -> dict:
        """发送文本消息"""
        token = await self.get_access_token()

        async with httpx.AsyncClient() as client:
            response = await client.post(
                f"https://qyapi.weixin.qq.com/cgi-bin/message/send?access_token={token}",
                json={
                    "touser": user_id,
                    "msgtype": "text",
                    "agentid": self.agent_id,
                    "text": {"content": content}
                },
                timeout=10.0
            )
            return response.json()

wework_service = WeWorkService()
```

**Step 2: 创建认证服务**

```python
# backend/app/services/auth_service.py
from datetime import datetime, timedelta
from jose import jwt
from passlib.context import CryptContext
from app.core.config import settings
from app.core.wework import wework_service
from app.models.user import User
from app.core.db import get_session
from sqlalchemy.ext.asyncio import AsyncSession

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

class AuthService:
    """认证服务"""

    @staticmethod
    def verify_password(plain_password: str, hashed_password: str) -> bool:
        return pwd_context.verify(plain_password, hashed_password)

    @staticmethod
    def get_password_hash(password: str) -> str:
        return pwd_context.hash(password)

    @staticmethod
    def create_access_token(data: dict) -> str:
        to_encode = data.copy()
        expire = datetime.utcnow() + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
        to_encode.update({"exp": expire})
        encoded_jwt = jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.ALGORITHM)
        return encoded_jwt

    async def wework_login(self, code: str, session: AsyncSession) -> str:
        """
        企业微信OAuth2登录

        流程:
        1. 用code换取用户信息
        2. 查询或创建用户
        3. 生成JWT token
        """
        # 获取企业微信用户信息
        wework_user = await wework_service.get_user_info(code)

        if wework_user.get("errcode") != 0:
            raise Exception(f"企业微信登录失败: {wework_user}")

        user_id = wework_user.get("userid")
        name = wework_user.get("name")
        avatar = wework_user.get("avatar")

        # 查询用户是否存在
        from sqlalchemy import select
        result = await session.execute(
            select(User).where(User.wework_id == user_id)
        )
        user = result.scalar_one_or_none()

        if not user:
            # 创建新用户
            user = User(
                wework_id=user_id,
                name=name,
                avatar=avatar,
                role="teacher",
                status=1
            )
            session.add(user)
            await session.commit()
            await session.refresh(user)

        # 生成token
        token = self.create_access_token(
            data={"sub": str(user.id), "role": user.role}
        )

        return token
```

**Step 3: 创建认证API**

```python
# backend/app/api/v1/auth.py
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from sqlalchemy.ext.asyncio import AsyncSession
from app.services.auth_service import AuthService
from app.core.db import get_session

router = APIRouter()

class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"

class WeWorkLoginRequest(BaseModel):
    code: str

@router.post("/wework", response_model=TokenResponse)
async def wework_login(
    request: WeWorkLoginRequest,
    session: AsyncSession = Depends(get_session)
):
    """
    企业微信OAuth2登录

    参数:
        code: 企业微信授权码
    """
    try:
        auth_service = AuthService()
        token = await auth_service.wework_login(request.code, session)
        return TokenResponse(access_token=token)
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))

@router.get("/test-token")
async def test_token():
    """测试token生成"""
    auth_service = AuthService()
    token = auth_service.create_access_token(data={"sub": "test", "role": "admin"})
    return {"token": token}
```

**Step 4: 创建数据库模型**

```python
# backend/app/models/user.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime

class User(SQLModel, table=True):
    __tablename__ = "users"

    id: Optional[int] = Field(default=None, primary_key=True)
    wework_id: str = Field(index=True, unique=True, max_length=64)
    name: str = Field(max_length=50)
    mobile: Optional[str] = Field(default=None, max_length=20)
    avatar: Optional[str] = Field(default=None, max_length=500)
    role: str = Field(default="teacher", max_length=20)  # admin, teacher, finance
    department_id: Optional[int] = None
    status: int = Field(default=1)  # 1:active 0:inactive

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)
```

```python
# backend/app/core/db.py
from sqlmodel import SQLModel, create_engine, Session
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from app.core.config import settings

# 异步引擎
engine = create_async_engine(
    settings.DATABASE_URL_DEV if settings.DEBUG else settings.DATABASE_URL,
    echo=settings.DEBUG,
)

async_session_maker = async_sessionmaker(
    engine, class_=AsyncSession, expire_on_commit=False
)

async def get_session() -> AsyncSession:
    async with async_session_maker() as session:
        yield session

async def init_db():
    """初始化数据库表"""
    async with engine.begin() as conn:
        await conn.run_sync(SQLModel.metadata.create_all)
```

**Step 5: 编写测试**

```python
# backend/tests/test_auth.py
import pytest
from app.services.auth_service import AuthService

def test_password_hash():
    """测试密码哈希"""
    auth_service = AuthService()
    password = "test123"
    hashed = auth_service.get_password_hash(password)

    assert auth_service.verify_password(password, hashed)
    assert not auth_service.verify_password("wrong", hashed)

def test_create_access_token():
    """测试token生成"""
    auth_service = AuthService()
    token = auth_service.create_access_token(data={"sub": "test", "role": "admin"})

    assert token is not None
    assert isinstance(token, str)
    assert len(token) > 0
```

**Step 6: 运行测试**

```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
pytest tests/test_auth.py -v
```

**验收标准:**
- [ ] FastAPI应用能正常启动
- [ ] /health接口返回{"status": "ok"}
- [ ] /docs能打开Swagger文档
- [ ] 企业微信OAuth2认证流程打通
- [ ] 密码哈希和验证功能正常
- [ ] JWT token生成和验证正常
- [ ] 基础测试通过

---

## Week 2: 数据模型层

### 任务 2.1: 创建所有数据模型

**Files:**
- Create: `backend/app/models/student.py`
- Create: `backend/app/models/course.py`
- Create: `backend/app/models/schedule.py`
- Create: `backend/app/models/contract.py`
- Create: `backend/app/models/payment.py`
- Create: `backend/app/models/attendance.py`
- Create: `backend/app/models/homework.py`
- Create: `backend/app/models/notification.py`
- Create: `backend/app/models/miniapp_user.py`

**Step 1: 创建学员模型**

```python
# backend/app/models/student.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime, date

class Student(SQLModel, table=True):
    __tablename__ = "students"

    id: Optional[int] = Field(default=None, primary_key=True)
    name: str = Field(max_length=50)
    nickname: Optional[str] = Field(default=None, max_length=50)
    gender: Optional[int] = None  # 1:male 2:female
    birthday: Optional[date] = None
    mobile: Optional[str] = Field(default=None, max_length=20)
    parent_name: Optional[str] = Field(default=None, max_length=50)
    parent_wework_id: Optional[str] = Field(default=None, max_length=64)
    parent_mobile: Optional[str] = Field(default=None, max_length=20)
    source: Optional[str] = Field(default=None, max_length=50)  # 来源
    status: int = Field(default=1)  # 1:潜在 2:在读 3:已流失
    tags: Optional[str] = None  # JSON数组
    notes: Optional[str] = None

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)
```

**Step 2: 创建课程和教室模型**

```python
# backend/app/models/course.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime
from decimal import Decimal

class Course(SQLModel, table=True):
    __tablename__ = "courses"

    id: Optional[int] = Field(default=None, primary_key=True)
    name: str = Field(max_length=100)
    category: Optional[str] = Field(default=None, max_length=50)
    color: str = Field(default="#409EFF", max_length=10)
    duration: int = Field(default=60)  # 分钟
    price: Optional[Decimal] = Field(default=None, decimal_places=2)
    max_students: int = Field(default=30)
    description: Optional[str] = None
    status: int = Field(default=1)  # 1:上架 2:下架

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)

class Classroom(SQLModel, table=True):
    __tablename__ = "classrooms"

    id: Optional[int] = Field(default=None, primary_key=True)
    name: str = Field(max_length=50)
    department_id: Optional[int] = None
    capacity: int = Field(default=30)
    equipment: Optional[str] = None  # JSON数组
    status: int = Field(default=1)  # 1:可用 2:维护中

    created_at: datetime = Field(default_factory=datetime.now)

class Department(SQLModel, table=True):
    __tablename__ = "departments"

    id: Optional[int] = Field(default=None, primary_key=True)
    name: str = Field(max_length=100)
    parent_id: Optional[int] = None
    manager_id: Optional[int] = None
    address: Optional[str] = Field(default=None, max_length=200)
    contact: Optional[str] = Field(default=None, max_length=50)
    status: int = Field(default=1)

    created_at: datetime = Field(default_factory=datetime.now)
```

**Step 3: 创建合同和缴费模型**

```python
# backend/app/models/contract.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime, date
from decimal import Decimal

class Contract(SQLModel, table=True):
    __tablename__ = "contracts"

    id: Optional[int] = Field(default=None, primary_key=True)
    contract_no: str = Field(index=True, unique=True, max_length=50)
    student_id: int = Field(foreign_key="students.id", index=True)
    course_id: Optional[int] = Field(default=None, foreign_key="courses.id")
    package_type: str = Field(max_length=20)  # 课时包类型
    total_hours: Decimal = Field(decimal_places=2)
    remaining_hours: Decimal = Field(decimal_places=2)
    unit_price: Decimal = Field(decimal_places=2)
    total_amount: Decimal = Field(decimal_places=2)
    received_amount: Decimal = Field(default=0, decimal_places=2)
    discount_amount: Decimal = Field(default=0, decimal_places=2)
    start_date: date
    end_date: Optional[date] = None
    expire_warning_days: int = Field(default=30)
    status: int = Field(default=1)  # 1:生效 2:完结 3:退费 4:过期
    contract_file: Optional[str] = Field(default=None, max_length=500)
    sales_id: Optional[int] = Field(default=None, foreign_key="users.id")
    notes: Optional[str] = None
    created_by: Optional[int] = Field(default=None, foreign_key="users.id")

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)
```

```python
# backend/app/models/payment.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime
from decimal import Decimal

class Payment(SQLModel, table=True):
    __tablename__ = "payments"

    id: Optional[int] = Field(default=None, primary_key=True)
    payment_no: str = Field(index=True, unique=True, max_length=50)
    contract_id: int = Field(foreign_key="contracts.id", index=True)
    amount: Decimal = Field(decimal_places=2)
    hours: Optional[Decimal] = Field(default=None, decimal_places=2)
    payment_method: int  # 1:微信 2:支付宝 3:现金 4:银行卡 5:转账
    payment_channel: Optional[str] = None  # wechat/alipay/cash/bank/transfer
    transaction_id: Optional[str] = None
    trade_no: Optional[str] = None
    payment_time: Optional[datetime] = None
    operator_id: Optional[int] = Field(default=None, foreign_key="users.id")
    status: int = Field(default=1)  # 1:待确认 2:已确认 3:已退款
    remark: Optional[str] = Field(default=None, max_length=500)

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)
```

**Step 4: 创建排课和考勤模型**

```python
# backend/app/models/schedule.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime

class Schedule(SQLModel, table=True):
    __tablename__ = "schedules"

    id: Optional[int] = Field(default=None, primary_key=True)
    course_id: int = Field(foreign_key="courses.id")
    teacher_id: int = Field(foreign_key="users.id")
    classroom_id: int = Field(foreign_key="classrooms.id")
    department_id: Optional[int] = None
    start_time: datetime
    end_time: datetime
    week_day: Optional[int] = None  # 1-7
    recurring_type: Optional[str] = None  # single/weekly/biweekly
    recurring_id: Optional[str] = None
    max_students: int = Field(default=30)
    enrolled_count: int = Field(default=0)
    status: int = Field(default=1)  # 1:已安排 2:已上课 3:已取消 4:已调课
    notes: Optional[str] = None
    created_by: Optional[int] = Field(default=None, foreign_key="users.id")

    created_at: datetime = Field(default_factory=datetime.now)
```

```python
# backend/app/models/attendance.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime
from decimal import Decimal

class Attendance(SQLModel, table=True):
    __tablename__ = "attendances"

    id: Optional[int] = Field(default=None, primary_key=True)
    schedule_id: int = Field(foreign_key="schedules.id", index=True)
    student_id: int = Field(foreign_key="students.id", index=True)
    contract_id: Optional[int] = Field(default=None, foreign_key="contracts.id")
    status: int  # 1:出勤 2:请假 3:缺勤 4:迟到
    check_time: Optional[datetime] = None
    check_method: int = Field(default=1)  # 1:手动 2:人脸 3:刷卡
    hours_consumed: Decimal = Field(default=1, decimal_places=2)
    notes: Optional[str] = None
    created_by: Optional[int] = Field(default=None, foreign_key="users.id")

    created_at: datetime = Field(default_factory=datetime.now)
```

**Step 5: 创建作业和通知模型**

```python
# backend/app/models/homework.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime

class Homework(SQLModel, table=True):
    __tablename__ = "homeworks"

    id: Optional[int] = Field(default=None, primary_key=True)
    schedule_id: int = Field(foreign_key="schedules.id")
    title: str = Field(max_length=200)
    content: Optional[str] = None
    images: Optional[str] = None  # JSON数组
    attachments: Optional[str] = None  # JSON数组
    deadline: Optional[datetime] = None
    publish_time: Optional[datetime] = None
    created_by: int = Field(foreign_key="users.id")
    status: int = Field(default=1)  # 1:草稿 2:已发布 3:已结束

    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)

class HomeworkSubmission(SQLModel, table=True):
    __tablename__ = "homework_submissions"

    id: Optional[int] = Field(default=None, primary_key=True)
    homework_id: int = Field(foreign_key="homeworks.id")
    student_id: int = Field(foreign_key="students.id")
    content: Optional[str] = None
    images: Optional[str] = None
    attachments: Optional[str] = None
    submit_time: Optional[datetime] = None
    score: Optional[int] = None  # 1-100
    feedback: Optional[str] = None
    graded_by: Optional[int] = Field(default=None, foreign_key="users.id")
    graded_at: Optional[datetime] = None
    status: int = Field(default=1)  # 1:待批改 2:已批改

    created_at: datetime = Field(default_factory=datetime.now)
```

```python
# backend/app/models/notification.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime

class Notification(SQLModel, table=True):
    __tablename__ = "notifications"

    id: Optional[int] = Field(default=None, primary_key=True)
    type: int  # 1:上课提醒 2:作业通知 3:考勤通知 4:合同通知 5:系统通知
    receiver_id: str = Field(max_length=64)
    receiver_type: int = Field(default=1)  # 1:企业微信 2:家长 3:小程序
    title: str = Field(max_length=200)
    content: Optional[str] = None
    url: Optional[str] = Field(default=None, max_length=500)
    sent_at: Optional[datetime] = None
    read_at: Optional[datetime] = None
    status: int = Field(default=0)  # 0:pending 1:sent 2:failed 3:read
    error_msg: Optional[str] = None

    created_at: datetime = Field(default_factory=datetime.now)
```

**Step 6: 创建小程序用户模型**

```python
# backend/app/models/miniapp_user.py
from typing import Optional
from sqlmodel import SQLModel, Field
from datetime import datetime

class MiniAppUser(SQLModel, table=True):
    __tablename__ = "miniapp_users"

    id: Optional[int] = Field(default=None, primary_key=True)
    openid: str = Field(index=True, unique=True, max_length=100)
    unionid: Optional[str] = Field(default=None, max_length=100)
    platform: str = Field(default="wechat")  # wechat/alipay
    student_id: Optional[int] = Field(default=None, foreign_key="students.id")
    nickname: Optional[str] = Field(default=None, max_length=50)
    avatar: Optional[str] = Field(default=None, max_length=500)
    phone: Optional[str] = Field(default=None, max_length=20)
    status: int = Field(default=1)
    last_login_at: Optional[datetime] = None

    created_at: datetime = Field(default_factory=datetime.now)
```

**Step 7: 创建模型导出**

```python
# backend/app/models/__init__.py
from app.models.user import User
from app.models.student import Student
from app.models.course import Course, Classroom, Department
from app.models.contract import Contract
from app.models.payment import Payment
from app.models.schedule import Schedule
from app.models.attendance import Attendance
from app.models.homework import Homework, HomeworkSubmission
from app.models.notification import Notification
from app.models.miniapp_user import MiniAppUser

__all__ = [
    "User",
    "Student",
    "Course",
    "Classroom",
    "Department",
    "Contract",
    "Payment",
    "Schedule",
    "Attendance",
    "Homework",
    "HomeworkSubmission",
    "Notification",
    "MiniAppUser",
]
```

**验收标准:**
- [ ] 所有20+张数据模型创建完成
- [ ] 模型之间外键关系正确
- [ ] 数据库表能正常创建
- [ ] 基础CRUD接口可运行

---

## Week 3: 课程管理

### 任务 3.1: 课程CRUD接口

**Files:**
- Create: `backend/app/schemas/course.py`
- Create: `backend/app/crud/course.py`
- Create: `backend/app/services/course_service.py`
- Create: `backend/app/api/v1/courses.py`

**Step 1: 创建课程Schema**

```python
# backend/app/schemas/course.py
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime
from decimal import Decimal

class CourseBase(BaseModel):
    name: str = Field(..., max_length=100)
    category: Optional[str] = None
    color: str = "#409EFF"
    duration: int = 60
    price: Optional[Decimal] = None
    max_students: int = 30
    description: Optional[str] = None
    status: int = 1

class CourseCreate(CourseBase):
    pass

class CourseUpdate(BaseModel):
    name: Optional[str] = None
    category: Optional[str] = None
    color: Optional[str] = None
    duration: Optional[int] = None
    price: Optional[Decimal] = None
    max_students: Optional[int] = None
    description: Optional[str] = None
    status: Optional[int] = None

class CourseResponse(CourseBase):
    id: int
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True
```

**Step 2: 创建课程CRUD**

```python
# backend/app/crud/course.py
from typing import List, Optional
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.course import Course

class CourseCRUD:
    @staticmethod
    async def get_all(session: AsyncSession, skip: int = 0, limit: int = 100) -> List[Course]:
        result = await session.execute(
            select(Course).offset(skip).limit(limit)
        )
        return result.scalars().all()

    @staticmethod
    async def get_by_id(session: AsyncSession, course_id: int) -> Optional[Course]:
        result = await session.execute(
            select(Course).where(Course.id == course_id)
        )
        return result.scalar_one_or_none()

    @staticmethod
    async def create(session: AsyncSession, data: dict) -> Course:
        course = Course(**data)
        session.add(course)
        await session.commit()
        await session.refresh(course)
        return course

    @staticmethod
    async def update(session: AsyncSession, course: Course, data: dict) -> Course:
        for key, value in data.items():
            if value is not None:
                setattr(course, key, value)
        await session.commit()
        await session.refresh(course)
        return course

    @staticmethod
    async def delete(session: AsyncSession, course: Course) -> None:
        await session.delete(course)
        await session.commit()
```

**Step 3: 创建课程API**

```python
# backend/app/api/v1/courses.py
from typing import List
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession
from app.schemas.course import CourseCreate, CourseUpdate, CourseResponse
from app.crud.course import CourseCRUD
from app.core.db import get_session

router = APIRouter()

@router.get("/", response_model=List[CourseResponse])
async def list_courses(
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=100),
    session: AsyncSession = Depends(get_session)
):
    """获取课程列表"""
    courses = await CourseCRUD.get_all(session, skip, limit)
    return courses

@router.get("/{course_id}", response_model=CourseResponse)
async def get_course(
    course_id: int,
    session: AsyncSession = Depends(get_session)
):
    """获取课程详情"""
    course = await CourseCRUD.get_by_id(session, course_id)
    if not course:
        raise HTTPException(status_code=404, detail="课程不存在")
    return course

@router.post("/", response_model=CourseResponse)
async def create_course(
    data: CourseCreate,
    session: AsyncSession = Depends(get_session)
):
    """创建课程"""
    course = await CourseCRUD.create(session, data.model_dump())
    return course

@router.put("/{course_id}", response_model=CourseResponse)
async def update_course(
    course_id: int,
    data: CourseUpdate,
    session: AsyncSession = Depends(get_session)
):
    """更新课程"""
    course = await CourseCRUD.get_by_id(session, course_id)
    if not course:
        raise HTTPException(status_code=404, detail="课程不存在")
    course = await CourseCRUD.update(session, course, data.model_dump(exclude_unset=True))
    return course

@router.delete("/{course_id}")
async def delete_course(
    course_id: int,
    session: AsyncSession = Depends(get_session)
):
    """删除课程"""
    course = await CourseCRUD.get_by_id(session, course_id)
    if not course:
        raise HTTPException(status_code=404, detail="课程不存在")
    await CourseCRUD.delete(session, course)
    return {"message": "删除成功"}
```

---

## Week 4: 学员管理

### 任务 4.1: 学员CRUD接口

**Files:**
- Create: `backend/app/schemas/student.py`
- Create: `backend/app/crud/student.py`
- Create: `backend/app/api/v1/students.py`

---

## Week 5: 合同缴费

### 任务 5.1: 合同管理接口

**Files:**
- Create: `backend/app/schemas/contract.py`
- Create: `backend/app/crud/contract.py`
- Create: `backend/app/api/v1/contracts.py`
- Create: `backend/app/services/contract_service.py`

### 任务 5.2: 缴费管理接口

**Files:**
- Create: `backend/app/schemas/payment.py`
- Create: `backend/app/crud/payment.py`
- Create: `backend/app/api/v1/payments.py`

---

## Week 6: 支付集成

### 任务 6.1: 微信支付集成

**Files:**
- Create: `backend/app/payment/wechat.py`
- Create: `backend/app/api/v1/payment/wechat.py`

### 任务 6.2: 支付宝集成

**Files:**
- Create: `backend/app/payment/alipay.py`
- Create: `backend/app/api/v1/payment/alipay.py`

### 任务 6.3: 支付回调处理

**Files:**
- Create: `backend/app/api/v1/payment/callback.py`

---

## Week 7: 排课考勤

### 任务 7.1: 排课管理

**Files:**
- Create: `backend/app/schemas/schedule.py`
- Create: `backend/app/crud/schedule.py`
- Create: `backend/app/services/schedule_service.py`
- Create: `backend/app/api/v1/schedules.py`

### 任务 7.2: 考勤管理

**Files:**
- Create: `backend/app/schemas/attendance.py`
- Create: `backend/app/crud/attendance.py`
- Create: `backend/app/api/v1/attendance.py`

---

## Week 8: 教师端

### 任务 8.1: 管理后台前端

**Files:**
- Create: `frontend/admin/src/api/course.js`
- Create: `frontend/admin/src/views/courses/Index.vue`
- Create: `frontend/admin/src/views/students/Index.vue`

### 任务 8.2: H5移动端

**Files:**
- Create: `frontend/h5/src/views/Schedule.vue`
- Create: `frontend/h5/src/views/Attendance.vue`

---

## Week 9: 消息系统

### 任务 9.1: 定时任务

**Files:**
- Create: `backend/app/tasks/reminders.py`
- Create: `backend/app/tasks/contract_expiry.py`
- Create: `backend/app/tasks/statistics.py`

### 任务 9.2: 消息推送服务

**Files:**
- Create: `backend/app/services/notification_service.py`
- Create: `backend/app/api/v1/notifications.py`

---

## Week 10: 小程序A（课表/考勤/作业）

### 任务 10.1: 小程序基础

**Files:**
- Create: `miniapp/src/App.vue`
- Create: `miniapp/manifest.json`

### 任务 10.2: 课表功能

**Files:**
- Create: `miniapp/src/pages/schedule/index.vue`
- Create: `miniapp/src/api/schedule.js`

### 任务 10.3: 考勤查看

**Files:**
- Create: `miniapp/src/pages/attendance/index.vue`

### 任务 10.4: 作业查看

**Files:**
- Create: `miniapp/src/pages/homework/index.vue`

---

## Week 11: 小程序BC（缴费/请假/调课）

### 任务 11.1: 在线缴费

**Files:**
- Create: `miniapp/src/pages/payment/index.vue`
- Create: `backend/app/api/v1/miniapp/payment.py`

### 任务 11.2: 请假调课

**Files:**
- Create: `miniapp/src/pages/leave/index.vue`
- Create: `backend/app/api/v1/miniapp/leave.py`

---

## Week 12: 测试部署

### 任务 12.1: 单元测试

**Files:**
- Create: `backend/tests/test_courses.py`
- Create: `backend/tests/test_students.py`
- Create: `backend/tests/test_contracts.py`

### 任务 12.2: 集成测试

**Files:**
- Create: `backend/tests/test_api.py`

### 任务 12.3: 部署配置

**Files:**
- Create: `deployment/Dockerfile`
- Create: `deployment/docker-compose.prod.yml`
- Create: `deployment/nginx.conf`

---

## 📋 实施检查清单

### 第1周检查点
- [ ] 项目结构创建完成
- [ ] FastAPI应用可运行
- [ ] 企业微信OAuth2集成完成
- [ ] JWT认证流程打通

### 第2周检查点
- [ ] 所有数据模型创建完成
- [ ] 数据库迁移脚本可用
- [ ] 基础CRUD接口可运行

### 第3-4周检查点
- [ ] 课程管理功能完成
- [ ] 学员管理功能完成
- [ ] 管理后台基础页面可用

### 第5-6周检查点
- [ ] 合同管理功能完成
- [ ] 缴费记录功能完成
- [ ] 微信支付集成完成
- [ ] 支付宝集成完成

### 第7-8周检查点
- [ ] 排课功能完成
- [ ] 考勤功能完成
- [ ] 教师端H5可用

### 第9周检查点
- [ ] 定时任务正常运行
- [ ] 消息推送功能完成
- [ ] 到期提醒功能完成

### 第10-11周检查点
- [ ] 小程序课表功能可用
- [ ] 小程序考勤查看可用
- [ ] 小程序作业查看可用
- [ ] 小程序缴费功能可用
- [ ] 小程序请假调课可用

### 第12周检查点
- [ ] 所有测试通过
- [ ] 生产环境部署完成
- [ ] 域名和SSL配置完成

---

## 📚 技术参考

### 企业微信API
- 授权登录: https://developer.work.weixin.qq.com/document/14598
- 消息推送: https://developer.work.weixin.qq.com/document/14594

### 微信支付
- JSAPI支付: https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml

### 支付宝支付
- 小程序支付: https://opendocs.alipay.com/open/02e7go

---

**文档版本**: 1.0
**创建时间**: 2026-02-14
